# Runtime dependencies -- human-editable source file.
# Run `pip-compile --generate-hashes requirements.txt -o requirements.lock` to regenerate the lockfile.
# Dockerfiles install from requirements.lock for supply chain integrity.
#
# Runtime dependencies
Django>=6.0.4,<7.0
pygments>=2.20.0  # CVE-2026-4539 fix (transitive dep)
dj-database-url>=3.1
psycopg[binary]>=3.3
gunicorn>=25.3
django-ninja>=1.6
Pillow>=12.2
curl_cffi>=0.15
recipe-scrapers>=15.11
beautifulsoup4>=4.14
openrouter>=0.8
whitenoise>=6.12
nh3>=0.3
cryptography>=46.0.7
jsonschema>=4.26
django-ratelimit>=4.1
django-ses>=4.7
webauthn>=2.7,<3.0

# Testing (needed for CI, optional for container)
pytest>=9.0
pytest-django>=4.12
pytest-asyncio>=1.3
pytest-cov>=7.1

# Dev/CI tools (installed separately via pipx or pre-commit)
# ruff - linting (pre-commit handles this)
# pip-audit - security scanning (CI only)
# bandit - SAST (CI only)
# detect-secrets - secret scanning (pre-commit handles this)
# radon - complexity checking (CI only)
